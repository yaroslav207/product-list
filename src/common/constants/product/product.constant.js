const MAX_PRICE_PRODUCT = 99999999.99;

const MIN_PRICE_PRODUCT = 0.01;

const MIN_DISCOUNT = 10;

const MAX_DISCOUNT = 90;

const MIN_LENGTH_NAME = 20;

const MAX_LENGTH_NAME = 60;

const MAX_LENGTH_DESCRIPTION = 200;

export {
  MAX_DISCOUNT,
  MIN_DISCOUNT,
  MAX_PRICE_PRODUCT,
  MIN_PRICE_PRODUCT,
  MAX_LENGTH_NAME,
  MIN_LENGTH_NAME,
  MAX_LENGTH_DESCRIPTION,
};
